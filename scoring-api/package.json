{
  "name": "scoring-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "streamlit run app_demo.py",
    "dev:full": "streamlit run app.py",
    "setup": "python -m venv .venv && pip install -r requirements.txt"
  },
  "description": "Carbon scoring API with Streamlit"
}
